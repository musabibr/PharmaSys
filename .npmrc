# Use Electron mirror for faster downloads on Windows (and corporate networks)
# Uncomment the line below if the official Electron download is slow or blocked:
# electron_mirror=https://npmmirror.com/mirrors/electron/

# Disable scripts for security (electron-builder installs its own app deps via postinstall)
# ignore-scripts=false
